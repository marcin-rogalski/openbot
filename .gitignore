node_modules
dist
dist-ssr
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea

# OS
.DS_Store
# Screenshots dropped into the repo root (root-level only; real assets live in subdirs)
/*.png

# Tauri / Rust
src-tauri/target
src-tauri/gen
Cargo.lock.bak

# Test / coverage
coverage

*storybook.log
storybook-static
